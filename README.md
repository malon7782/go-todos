Unfortunately, malon7782 (author of this project) is dumb.  
Use `go run .` to run instead of `todos` (which is written in the promts but don't trust 'em)  

Roadmap:
 - Add a few more commands
 - More information for each item
 - Support colors

0% ai
